<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ev.mes.dao.ProcessCheckDao">

	<select id="get" resultType="com.ev.mes.domain.ProcessCheckDO">
		SELECT
		<include refid="sql_column_list"/>
		FROM mes_process_check
		WHERE id = #{value}
	</select>

	<select id="list" resultType="com.ev.mes.domain.ProcessCheckDO">
		SELECT
		<include refid="sql_column_list"/>
		FROM mes_process_check
		<include refid="sql_condition"/>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
			<otherwise>
				order by id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="count" resultType="int">
		select count(*) from mes_process_check
		<include refid="sql_condition"/>
	</select>

	<insert id="save" parameterType="com.ev.mes.domain.ProcessCheckDO" useGeneratedKeys="true" keyProperty="id">
		insert into mes_process_check
		(
			`foreign_id`,
			`pro_id`,
			`type`,
			`whether_check`,
			`remark`,
			`create_by`,
			`create_time`,
			`update_by`,
			`update_time`,
			`del_flag`
		)
		values
		(
			#{foreignId},
			#{proId},
			#{type},
			#{whetherCheck},
			#{remark},
			#{createBy},
			#{createTime},
			#{updateBy},
			#{updateTime},
			#{delFlag}
		)
	</insert>

	<update id="update" parameterType="com.ev.mes.domain.ProcessCheckDO">
		update mes_process_check
		<set>
			<if test="foreignId != null">`foreign_id` = #{foreignId}, </if>
			<if test="proId != null">`pro_id` = #{proId}, </if>
			<if test="type != null">`type` = #{type}, </if>
			<if test="whetherCheck != null">`whether_check` = #{whetherCheck}, </if>
			<if test="remark != null">`remark` = #{remark}, </if>
			<if test="createBy != null">`create_by` = #{createBy}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateBy != null">`update_by` = #{updateBy}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}, </if>
			<if test="delFlag != null">`del_flag` = #{delFlag}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="remove">
		delete from mes_process_check where id = #{value}
	</delete>

	<delete id="batchRemove">
		delete from mes_process_check where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<sql id="sql_column_list">
		`id`,`foreign_id`,`pro_id`,`type`,`whether_check`,	`remark`,`create_by`,`create_time`,`update_by`,`update_time`,`del_flag`
	</sql>

	<sql id="sql_condition">
		<where>
			<if test="id != null and id != ''"> and id = #{id} </if>
			<if test="foreignId != null and foreignId != ''"> and foreign_id = #{foreignId} </if>
			<if test="proId != null and proId != ''"> and pro_id = #{proId} </if>
			<if test="type != null and type != ''"> and type = #{type} </if>
			<if test="whetherCheck != null and whetherCheck != ''"> and whether_check = #{whetherCheck} </if>
			<if test="remark != null and remark != ''"> and remark = #{remark} </if>
			<if test="createBy != null and createBy != ''"> and create_by = #{createBy} </if>
			<if test="createTime != null and createTime != ''"> and create_time = #{createTime} </if>
			<if test="updateBy != null and updateBy != ''"> and update_by = #{updateBy} </if>
			<if test="updateTime != null and updateTime != ''"> and update_time = #{updateTime} </if>
			<if test="delFlag != null and delFlag != ''"> and del_flag = #{delFlag} </if>
		</where>
	</sql>



	<delete id="removeByProcessId">
		delete
		from
		 mes_process_check
		 where
		 foreign_id = #{foreignId}
		 AND
		 type=#{type}
	</delete>



	<delete id="removeBacthById">
		delete
		from
		mes_process_check
		where
		type=#{type}
		and
		id in
		<foreach item="item" collection="id" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<delete id="removeBacthByforeignId">
		delete
		from
		mes_process_check
		where
		type=#{type}
		and
		foreign_id in
		<foreach item="item" collection="foreignId" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>


	<select id="getDetailByProcessId" resultType="map">
		SELECT
			a.id,
			a.foreign_id as foreignId,
			a.whether_check as whetherCheck,
			a.pro_id as proId,
			b.name as proName,
			a.remark,
			b.code,
			b.norm,
			b.method,
			b.instrument_name as instrument ,
			b.remark
		FROM mes_process_check a
		LEFT JOIN mes_check_project b on a.pro_id=b.id
		WHERE
		  a.foreign_id=#{foreignId}
		and
		  a.type=#{type}
	</select>



	
</mapper>