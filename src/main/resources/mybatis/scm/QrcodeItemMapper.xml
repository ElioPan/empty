<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ev.scm.dao.QrcodeItemDao">

	<select id="get" resultType="com.ev.scm.domain.QrcodeItemDO">
		SELECT 
		<include refid="sql_column_list"/>  
		FROM scm_qrcode_item 
		WHERE id = #{value}
	</select>

	<select id="list" resultType="com.ev.scm.domain.QrcodeItemDO">
		SELECT 
		<include refid="sql_column_list"/>
		FROM scm_qrcode_item
		<include refid="sql_condition"/>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                ORDER BY ${sort} ${order}
            </when>
			<otherwise>
                ORDER BY id DESC
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="count" resultType="int">
        SELECT COUNT(*) FROM scm_qrcode_item
		<include refid="sql_condition"/>
	</select>

    <sql id="sql_column_list">
		`id`,`qrcode_id`,`in_out_type`,`inhead_id`,`inhead_code`,`inbody_id`,`count`,`create_by`,`create_time`,`update_by`,`update_time`,`del_flag`
	</sql>

    <sql id="sql_condition">
        <where>
                            <if test="id != null and id != ''"> AND id = #{id} </if>
                            <if test="qrcodeId != null and qrcodeId != ''"> AND qrcode_id = #{qrcodeId} </if>
                            <if test="inOutType != null and inOutType != ''"> AND in_out_type = #{inOutType} </if>
                            <if test="inheadId != null and inheadId != ''"> AND inhead_id = #{inheadId} </if>
							<if test="inheadCode != null and inheadCode != ''"> AND inhead_code = #{inheadCode} </if>
                            <if test="inbodyId != null and inbodyId != ''"> AND inbody_id = #{inbodyId} </if>
                            <if test="count != null and count != ''"> AND count = #{count} </if>
                            <if test="createBy != null and createBy != ''"> AND create_by = #{createBy} </if>
                            <if test="createTime != null and createTime != ''"> AND create_time = #{createTime} </if>
                            <if test="updateBy != null and updateBy != ''"> AND update_by = #{updateBy} </if>
                            <if test="updateTime != null and updateTime != ''"> AND update_time = #{updateTime} </if>
                            <if test="delFlag != null and delFlag != ''"> AND del_flag = #{delFlag} </if>
                    </where>
    </sql>
	 
	<insert id="save" parameterType="com.ev.scm.domain.QrcodeItemDO" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO scm_qrcode_item
		(
			`qrcode_id`, 
			`in_out_type`, 
			`inhead_id`,
			`inhead_code`,
			`inbody_id`, 
			`count`, 
			`create_by`, 
			`create_time`, 
			`update_by`, 
			`update_time`, 
			`del_flag`
		)
		VALUES
		(
			#{qrcodeId}, 
			#{inOutType}, 
			#{inheadId},
			#{inheadCode},
			#{inbodyId}, 
			#{count}, 
			#{createBy}, 
			#{createTime}, 
			#{updateBy}, 
			#{updateTime}, 
			#{delFlag}
		)
	</insert>

	<insert id="batchInsert" parameterType="List" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO scm_qrcode_item
		(
			`qrcode_id`,
			`in_out_type`,
			`inhead_id`,
		    `inhead_code`,
			`inbody_id`,
			`count`,
			`create_by`,
			`create_time`,
			`update_by`,
			`update_time`,
			`del_flag`
		)
		VALUES
		<foreach collection="list" item="obj" separator=",">
		(
			#{obj.qrcodeId},
			#{obj.inOutType},
			#{obj.inheadId},
			#{obj.inheadCode},
			#{obj.inbodyId},
			#{obj.count},
			#{obj.createBy},
			#{obj.createTime},
			#{obj.updateBy},
			#{obj.updateTime},
			#{obj.delFlag}
		)
		</foreach>
	</insert>
	 
	<update id="update" parameterType="com.ev.scm.domain.QrcodeItemDO">
		UPDATE scm_qrcode_item
		<set>
			<if test="qrcodeId != null">`qrcode_id` = #{qrcodeId}, </if>
			<if test="inOutType != null">`in_out_type` = #{inOutType}, </if>
			<if test="inheadId != null">`inhead_id` = #{inheadId}, </if>
			<if test="inheadCode != null">`inhead_code` = #{inheadCode}, </if>
			<if test="inbodyId != null">`inbody_id` = #{inbodyId}, </if>
			<if test="count != null">`count` = #{count}, </if>
			<if test="createBy != null">`create_by` = #{createBy}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateBy != null">`update_by` = #{updateBy}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}, </if>
			<if test="delFlag != null">`del_flag` = #{delFlag}</if>
		</set>
		WHERE id = #{id}
	</update>
	
	<delete id="remove">
		DELETE FROM scm_qrcode_item WHERE id = #{value}
	</delete>
	
	<delete id="batchRemove">
        DELETE FROM scm_qrcode_item WHERE id IN
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	
</mapper>