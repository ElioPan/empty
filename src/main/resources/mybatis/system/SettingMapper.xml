<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ev.system.dao.SettingDao">

	<select id="get" resultType="com.ev.system.domain.SettingDO">
		SELECT 
		<include refid="sql_column_list"/>  
		FROM sys_setting 
		WHERE `key` = #{value}
	</select>

	<select id="list" resultType="com.ev.system.domain.SettingDO">
		SELECT 
		<include refid="sql_column_list"/>
		FROM sys_setting
		<include refid="sql_condition"/>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                ORDER BY ${sort} ${order}
            </when>
			<otherwise>
                ORDER BY `key` DESC
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="count" resultType="int">
        SELECT COUNT(*) FROM sys_setting
		<include refid="sql_condition"/>
	</select>

    <sql id="sql_column_list">
		`key`,`value`,`create_by`,`create_time`,`update_by`,`update_time`,`del_flag`
	</sql>

    <sql id="sql_condition">
        <where>
                            <if test="key != null and key != ''"> AND `key` = #{key} </if>
                            <if test="value != null and value != ''"> AND value = #{value} </if>
                            <if test="createBy != null and createBy != ''"> AND create_by = #{createBy} </if>
                            <if test="createTime != null and createTime != ''"> AND create_time = #{createTime} </if>
                            <if test="updateBy != null and updateBy != ''"> AND update_by = #{updateBy} </if>
                            <if test="updateTime != null and updateTime != ''"> AND update_time = #{updateTime} </if>
                            <if test="delFlag != null and delFlag != ''"> AND del_flag = #{delFlag} </if>
                    </where>
    </sql>
	 
	<insert id="save" parameterType="com.ev.system.domain.SettingDO">
		INSERT INTO sys_setting
		(
			`key`, 
			`value`, 
			`create_by`, 
			`create_time`, 
			`update_by`, 
			`update_time`, 
			`del_flag`
		)
		VALUES
		(
			#{key}, 
			#{value}, 
			#{createBy}, 
			#{createTime}, 
			#{updateBy}, 
			#{updateTime}, 
			#{delFlag}
		)
	</insert>
	 
	<update id="update" parameterType="com.ev.system.domain.SettingDO">
		UPDATE sys_setting
		<set>
			<if test="value != null">`value` = #{value}, </if>
			<if test="createBy != null">`create_by` = #{createBy}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateBy != null">`update_by` = #{updateBy}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}, </if>
			<if test="delFlag != null">`del_flag` = #{delFlag}</if>
		</set>
		WHERE `key` = #{key}
	</update>

	<update id="updateAll" parameterType="com.ev.system.domain.SettingDO">
		UPDATE sys_setting SET
			`value` = #{value}, 
			`create_by` = #{createBy}, 
			`create_time` = #{createTime}, 
			`update_by` = #{updateBy}, 
			`update_time` = #{updateTime}, 
			`del_flag` = #{delFlag}
		WHERE `key` = #{key}
	</update>

	
	<delete id="remove">
		DELETE FROM sys_setting WHERE `key` = #{value}
	</delete>
	
	<delete id="batchRemove">
        DELETE FROM sys_setting WHERE `key` IN
		<foreach item="key" collection="array" open="(" separator="," close=")">
			#{key}
		</foreach>
	</delete>
	
</mapper>