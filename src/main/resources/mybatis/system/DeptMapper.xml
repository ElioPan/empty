<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ev.system.dao.DeptDao">

	<select id="get" resultType="com.ev.system.domain.DeptDO">
		select
		`dept_id`,`parent_id`,`name`,`order_num`,`del_flag`,`type`,`id_path` from sys_dept
		where dept_id = #{value}
	</select>

	<select id="list" resultType="com.ev.system.domain.DeptDO">
		select `dept_id`,`parent_id`,`name`,`order_num`,`del_flag`,`type`,`id_path` from
		sys_dept
		<where>
		    <if test="isContainChildren != null and isContainChildren == 0">
				<if test="deptId != null and deptId != ''"> and dept_id = #{deptId} </if>
			</if>
		    <if test="isContainChildren == null or isContainChildren == 1">
				<if test="deptId != null and deptId != ''"> and id_path like CONCAT ('%/',#{deptId},'/%') </if>
			</if>

			<if test="parentId != null and parentId != ''"> and parent_id = #{parentId} </if>
			<if test="name != null and name != ''"> and name like CONCAT ('%',#{name},'%') </if>
			<if test="orderNum != null and orderNum != ''"> and order_num = #{orderNum} </if>
			<if test="delFlag != null and delFlag != ''"> and del_flag = #{delFlag} </if>
			<if test="type != null and type != ''"> and type = #{type} </if>
			<if test="idPath != null and idPath != ''"> and id_path like CONCAT(CONCAT('%',#{idPath}),'%') </if>

			<if test="deptNames != null and deptNames.size()>0"> and `name` IN
				<foreach item="item" collection="deptNames" open="(" separator="," close=")" index="index">
					<if test="item!=null">
						#{item}
					</if>
				</foreach>
			</if>
		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
			<otherwise>
				order by order_num asc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="count" resultType="int">
		select count(*) from sys_dept
		<where>
			<if test="deptId != null and deptId != ''"> and dept_id = #{deptId} </if>
			<if test="parentId != null and parentId != ''"> and parent_id = #{parentId} </if>
			<if test="name != null and name != ''"> and name = #{name} </if>
			<if test="orderNum != null and orderNum != ''"> and order_num = #{orderNum} </if>
			<if test="delFlag != null and delFlag != ''"> and del_flag = #{delFlag} </if>
			<if test="type != null and type != ''"> and type = #{type} </if>
			<if test="idPath != null and idPath != ''"> and id_path like CONCAT(CONCAT('%',#{idPath}),'%') </if>
		</where>
	</select>

	<insert id="save" parameterType="com.ev.system.domain.DeptDO"
		useGeneratedKeys="true" keyProperty="deptId">
		insert into sys_dept
		(
		`parent_id`,
		`name`,
		`order_num`,
		`del_flag`,
		`type`,
		 `id_path`
		)
		values
		(
		#{parentId},
		#{name},
		#{orderNum},
		#{delFlag},
		#{type},
		#{idPath}
		)
	</insert>

	<update id="update" parameterType="com.ev.system.domain.DeptDO">
		update sys_dept
		<set>
			<if test="parentId != null">`parent_id` = #{parentId}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}, </if>
			<if test="delFlag != null">`del_flag` = #{delFlag},</if>
			<if test="type != null">`type` = #{type},</if>
			<if test="idPath != null">`id_path` = #{idPath}</if>
		</set>
		where dept_id = #{deptId}
	</update>

	<delete id="remove">
		delete from sys_dept where dept_id = #{value}
	</delete>

	<delete id="batchRemove">
		delete from sys_dept where dept_id in
		<foreach item="deptId" collection="array" open="(" separator=","
			close=")">
			#{deptId}
		</foreach>
	</delete>
	
	<select id="listParentDept" resultType="long">
		select DISTINCT parent_id from sys_dept
	</select>

    <select id="getDeptUserNumber" resultType="int">
		select count(*) from sys_user where dept_id = #{value}
	</select>


	<select id="getIdpathByUserId" resultType="com.ev.system.domain.DeptDO">
		 select
		 id_path
		 from
		 sys_dept
		 where
		 dept_id=(select dept_id from sys_user WHERE user_id =#{VALUE})
  	</select>



</mapper>